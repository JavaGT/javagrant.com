extends ./main.pug
block content
  h1 Welcome.
  p.
    I am Java, these are my blogs!
  for blog in blogs
    if (!blog.config.hidden)
      h1
        a(href='/blog/'+blog.config.slug)=blog.config.title||blog.config.slug

